# The structure of pattern file to search
# FIRST COLUMN is pattern to be identify SQL command
# Second COLUMN is pattern to look for in the SQL command body
# If second column nedd more keywords they must be divided by .* (means anything between for grep utility)
# The pattern must be in UPPER CASE
# Patterns are divided by space
# There is assumption that SQL command is enclosed by ;
# The line with remark must begins with #

USER PROFILE.*DEFAULT
GRANT DBA
NOAUDIT
